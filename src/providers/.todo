# Provider scaffold — status

## Done (WP-003)
- [x] move binance to depth::binance::spot (`src/providers/binance/spot.rs`)
- [x] move binance-usd-m to depth::binance::usd_m (`src/providers/binance/usd_m.rs`)
- [x] `BookSource::parse` accepts arbitrary provider labels (maps to `Provider::Other`)
- [x] third venue can register via `--sources provider:SYMBOL` without code changes
- [x] `BookSource` preserves original provider label for correct stream routing

## Done (WP-004)
- [x] add rpi support — RPI stream routing (`binance-usd-m:RPI:SYMBOL`) end-to-end
- [x] RPI overlay stays out of canonical global merge unless explicitly subscribed
- [x] TUI Δ tab shows `@depth − @rpiDepth` per price without polluting canonical merge
- [x] RPI subscription behavior documented in WORKPLAN.md

## Future
- [ ] provider registry trait: allow external crates to register Endpoints<Depth> at runtime
- [ ] kraken / coinbase example provider skeleton
