# Provider expansion backlog (WP-003 scaffold)

## Layout

- `depth::binance::spot` — Binance spot (done)
- `binance_usd_m` — still at crate root; target `depth::binance::usd_m`
- `depth::other` — third-venue scaffold (parse + stub Endpoints; no WebSocket)

## Remaining

- [ ] move `binance_usd_m` to `depth::binance::usd_m`
- [ ] add rpi support (WP-004; keep RPI optional on USDM)
- [ ] wire `Provider::Other` (or per-label registry) into `run_global_book_stream` when a real venue lands
- [ ] document new venue checklist: module under `depth/`, `REGISTERED_LABELS`, `Provider::from_label`, multiplexor match arm
