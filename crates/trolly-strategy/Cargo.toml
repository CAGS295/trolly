[package]
name = "trolly-strategy"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
trolly-stream = { path = "../trolly-stream" }

[dev-dependencies]
trolly-stream = { path = "../trolly-stream" }
